import { ZoomCarteDirective } from './zoom-carte.directive';

describe('ZoomCarteDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomCarteDirective();
    expect(directive).toBeTruthy();
  });
});
